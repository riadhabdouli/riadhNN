{% extends 'base.html.twig' %}

{% block title %}District{% endblock %}

{% block body %}
    <h1>District</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDistrict</th>
                <td>{{ district.idDistrict }}</td>
            </tr>
            <tr>
                <th>NomDistrict</th>
                <td>{{ district.nomDistrict }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('district_index') }}">back to list</a>

    <a href="{{ path('district_edit', {'idDistrict': district.idDistrict}) }}">edit</a>

    {{ include('district/_delete_form.html.twig') }}
{% endblock %}
