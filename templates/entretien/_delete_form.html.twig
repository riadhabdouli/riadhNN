<form method="post" action="{{ path('entretien_delete', {'id': entretien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entretien.id) }}">
    <button class="btn">Delete</button>
</form>
