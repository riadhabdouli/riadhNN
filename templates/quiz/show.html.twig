{% extends 'backend.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quizid</th>
                <td>{{ quiz.quizid }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ quiz.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quiz_index') }}">back to list</a>

    <a href="{{ path('quiz_edit', {'quizid': quiz.quizid}) }}">edit</a>

    {{ include('quiz/_delete_form.html.twig') }}
{% endblock %}
