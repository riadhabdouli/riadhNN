<form method="post" action="{{ path('fonction_principale_delete', {'idFonction': fonction_principale.idFonction}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fonction_principale.idFonction) }}">
    <button class="btn">Delete</button>
</form>
