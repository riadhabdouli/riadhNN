<form method="post" action="{{ path('formation_delete', {'idFormation': formation.idFormation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.idFormation) }}">
    <button class="btn">Delete</button>
</form>
