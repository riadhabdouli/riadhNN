<form method="post" action="{{ path('question_delete', {'questionId': question.questionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.questionId) }}">
    <button class="btn">Delete</button>
</form>
